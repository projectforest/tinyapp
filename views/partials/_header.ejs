<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
  <link type="text/css" rel="stylesheet" href="/bootstrap.min.css">
</head>
<body>
  <div class="container">
    <div class="col-xs-12 bg-info" style="height: 100px;">
      <a href="/urls"><h1>TinyApp</h1></a>
    </div>
    <% if(!users.hasOwnProperty(user)) { %>
      <table class="table">
        <tr>
          <td><a href="/login">Login</a> (or <a href="/register">register</a>) to continue</td>
        </tr>
      </table>
    <% } else { %>
      <table class="table">
        <tr>
          <td>
            <form class="form-inline navbar-form" method="post" action="/logout">
              <div class="form-group">
                <%= users[user].email %>
              </div>
              <button type="submit" class="btn btn-default">Logout</button>
            </form>
          </td>
          <td>
            <h5><a href="/urls">My Links</a></h5>
            <h5><a href="/urls/new">Shorten</a> a URL</h5>
          </td>
        </tr>
      </table>
    <% } %>