<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
  <link type="text/css" rel="stylesheet" href="/bootstrap.min.css">
</head>
<body>
  <div class="container">
    <div class="col-xs-12 bg-info" style="height: 100px;">
      <a href="/urls"><h1>TinyApp</h1></a>
    </div>
    <div class="col-md-3"> <h3>Shorten <a href="/urls/new">a new URL</a></h3></div>
    <div class="col-md-5 pull-right">
      <% if(!users.hasOwnProperty(user)) { %>
        <form class="navbar-form" method="get" action="/login">
          <button type="submit" class="btn btn-default">Login</button>
        </form>
        <form class="form-inline navbar-form" method="get" action="/register">
          <button type="submit" class="btn btn-default">Register</button>
        </form>
        <% } else { %>
        <form class="navbar-form" method="post" action="/logout">
          <div class="form-group">
            <%= users[user].email %>
          </div>
          <button type="submit" class="btn btn-default">Logout</button>
        </form>
      <% } %>
    </div>